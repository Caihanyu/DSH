# dependencies — never commit
node_modules/

# TypeScript incremental build caches
*.tsbuildinfo

# logs
*.log
server.log
server.log.err

# OS
.DS_Store
Thumbs.db

# sensitive local state (never upload)
.credentials.yaml
state.json

# plugin / builder local dev installs (registry mirrors and lock files)
plugins/*/.npmrc
plugins/*/package-lock.json
plugins/*/.typecheck-head/
build/.npmrc
build/package-lock.json
