# dependencies — never commit
node_modules/

# TypeScript incremental build caches
*.tsbuildinfo

# logs
*.log
server.log
server.log.err

# OS
.DS_Store
Thumbs.db

# sensitive local state (never upload)
.credentials.yaml
state.json
